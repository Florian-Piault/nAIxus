# Claude overrides

Configuration spécifique à Claude installable depuis `harness/claude`.

Les IDs de sélection prennent la forme `harness/claude/<nom>` et peuvent être utilisés avec `install --include`, `install --exclude`, `sync --include` et `sync --exclude`.

Voir [`../../docs/commands.md`](../../docs/commands.md).
