# Codex overrides

Configuration spécifique à Codex installable depuis `harness/codex`.

Les IDs de sélection prennent la forme `harness/codex/<nom>` et peuvent être utilisés avec `install --include`, `install --exclude`, `sync --include` et `sync --exclude`.

Voir [`../../docs/commands.md`](../../docs/commands.md).
