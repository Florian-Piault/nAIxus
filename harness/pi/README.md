# Pi overrides

Configuration spécifique à Pi installable depuis `harness/pi`.

Les IDs de sélection prennent la forme `harness/pi/<nom>` et peuvent être utilisés avec `install --include`, `install --exclude`, `sync --include` et `sync --exclude`.

Voir [`../../docs/commands.md`](../../docs/commands.md).
