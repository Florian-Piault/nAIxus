# Shared context

Contexte partagé installable depuis `core/context`.

Les IDs de sélection prennent la forme `core/context/<nom>` et peuvent être utilisés avec `install --include`, `install --exclude`, `sync --include` et `sync --exclude`.

Voir [`../../docs/commands.md`](../../docs/commands.md).
