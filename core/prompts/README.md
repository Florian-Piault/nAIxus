# Shared prompts

Prompts partagés installables depuis `core/prompts`.

Les IDs de sélection prennent la forme `core/prompts/<nom>` et peuvent être utilisés avec `install --include`, `install --exclude`, `sync --include` et `sync --exclude`.

Voir [`../../docs/commands.md`](../../docs/commands.md).
